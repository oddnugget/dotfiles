- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: 
      path: .tmux.conf
      force: true
      
    ~/.zshrc: zshrc
    ~/.wezterm.lua: 
      path: .wezterm.lua
      create: true
    
    ~/.config/nvim: 
      path: nvim
      force: true

- shell:
  - [git submodule update --init --recursive, Installing Submodules]


