- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.config/nvim: 
      path: nvim
      force: true

- shell:
  - [git submodule update --init --recursive, Installing Submodules]


